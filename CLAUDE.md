# oh-my-peekly

이 저장소에서 새 세션을 시작하면, 다른 작업을 하기 전에 **`docs/worklog/` 안에서 가장 최근 날짜의 파일을 먼저 읽어라.** 어제까지 뭘 했는지, 뭐가 끝났고 뭐가 남았는지, 최근에 어떤 결정을 왜 내렸는지가 거기 정리되어 있다. 여러 날짜 파일이 있으면 최소 최근 2~3일치는 훑어서 흐름을 파악한다.

개발 규칙(기술 스택, 커밋/릴리즈 방식, 플러그인 패키징, worklog 작성 규칙 등)은 `docs/CONVENTIONS.md`를 따른다 — 특히 그 문서 10번 항목: **이 저장소에 그날 커밋이 하나라도 생기면, 세션을 마무리하기 전에 `docs/worklog/<YYYY-MM-DD>.md`를 사용자가 요청하지 않아도 자동으로 작성/갱신한다.**
